% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toddler_functions.r
\name{toddler_wide}
\alias{toddler_wide}
\title{Make data wider (untidy) like a toddler spreading toys everywhere}
\usage{
toddler_wide(data, names_from, values_from, id_cols = NULL)
}
\arguments{
\item{data}{A data frame (preferably a nice, tidy one)}

\item{names_from}{Column name (character) to get names from}

\item{values_from}{Column name (character) to get values from}

\item{id_cols}{Character vector of ID columns (optional). If NULL, uses all other columns}
}
\value{
A wider, messier data frame that would make Hadley Wickham weep
}
\description{
Takes your beautifully tidy data and spreads it wide like a toddler with
a bag of toys. Perfect for teaching students why we love tidy data.
}
\examples{
# Create some tidy data
tidy_data <- data.frame(
  id = rep(1:3, each = 2),
  measure = rep(c("height", "weight"), 3),
  value = c(170, 70, 165, 65, 180, 80)
)

# Now make it wide and unwieldy
toddler_wide(tidy_data, "measure", "value", "id")
}
