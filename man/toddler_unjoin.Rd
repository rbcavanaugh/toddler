% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toddler_functions.r
\name{toddler_unjoin}
\alias{toddler_unjoin}
\title{Split data like a toddler "sharing" their toys}
\usage{
toddler_unjoin(
  data,
  by_rows = NULL,
  by_cols = FALSE,
  by_groups = NULL,
  id_cols = NULL,
  cols1 = NULL,
  prop = 0.5,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame that's about to be dismantled}

\item{by_rows}{Column name (character) to split rows by}

\item{by_cols}{Logical. Split columns instead of rows (horizontal chaos)}

\item{by_groups}{Column name (character) to split into groups}

\item{id_cols}{Character vector of ID columns to keep when splitting columns}

\item{cols1}{Character vector of columns for first dataframe when splitting columns}

\item{prop}{Proportion for first group when splitting rows (default 0.5)}

\item{seed}{Random seed for reproducible splitting tantrums}
}
\value{
A list of data frames, because who needs everything in one place?
}
\description{
Takes a perfectly good data frame and splits it into pieces like a toddler
dividing up their snacks (spoiler: the portions won't be equal).
}
\examples{
df <- data.frame(id = 1:10, name = letters[1:10], value = runif(10))

# Split rows randomly
toddler_unjoin(df, by_rows = "id", prop = 0.7)

# Split columns (keeping id in both)
toddler_unjoin(df, by_cols = TRUE, id_cols = "id", cols1 = "name")
}
