% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toddler_functions.r
\name{toddler_inconsistent}
\alias{toddler_inconsistent}
\title{Make categorical values inconsistent like toddler pronunciation}
\usage{
toddler_inconsistent(data, cols = NULL, seed = NULL)
}
\arguments{
\item{data}{A data frame with boringly consistent categories}

\item{cols}{Column names (character vector) to make inconsistent.
If NULL, targets all character columns because chaos should be democratic.}

\item{seed}{Random seed for reproducible linguistic disasters}
}
\value{
A data frame where "Yes" might also be "YES", "y", or "True"
}
\description{
Takes perfectly consistent categorical data and makes it as inconsistent
as a toddler trying to say "spaghetti" (is it "pasketti"? "sketty"? who knows!).
}
\examples{
df <- data.frame(
  answer = rep(c("yes", "no"), 5),
  gender = rep(c("male", "female"), 5)
)
toddler_inconsistent(df)
}
