% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toddler_functions.R
\name{toddler_units}
\alias{toddler_units}
\title{Add units to numeric values like a toddler's helpful "corrections"}
\usage{
toddler_units(
  data,
  cols = NULL,
  units = c("kg", "lbs", "cm", "in", "sec", "min"),
  prop = 0.3,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame with naked numbers}

\item{cols}{Column names (character vector) to add units to.
If NULL, targets all numeric columns because every number deserves a buddy.}

\item{units}{Vector of possible units to add. Mix and match for maximum confusion!}

\item{prop}{Proportion of values to affect (0-1)}

\item{seed}{Random seed for reproducible unit chaos}
}
\value{
A data frame where 25 might become "25kg" or "25sec" regardless of context
}
\description{
Because toddlers think every number needs a friend, this function adds
units to your numeric data whether they make sense or not.
}
\examples{
df <- data.frame(height = c(170, 165, 180), age = c(25, 30, 35))
toddler_units(df, units = c("cm", "kg", "years"), prop = 0.5)
}
