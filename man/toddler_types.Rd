% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toddler_functions.R
\name{toddler_types}
\alias{toddler_types}
\title{Mix data types like a toddler mixing food groups}
\usage{
toddler_types(data, cols = NULL, prop = 0.1, seed = NULL)
}
\arguments{
\item{data}{A data frame with well-behaved numeric columns}

\item{cols}{Column names (character vector) to mess up types.
If NULL, targets numeric columns because numbers are too orderly.}

\item{prop}{Proportion of values to affect (0-1)}

\item{seed}{Random seed for reproducible type confusion}
}
\value{
A data frame where numbers get mixed with "N/A", "missing", and "NULL"
}
\description{
Takes your nice numeric columns and sprinkles in some text values like
a toddler putting ketchup on ice cream - technically possible but oh so wrong.
}
\examples{
df <- data.frame(age = c(25, 30, 35), weight = c(70, 80, 75))
toddler_types(df, prop = 0.2)
}
